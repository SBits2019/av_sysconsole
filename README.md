to run this do the following:

1. git clone https://git

2. run virtualenv after cd into cloned directory
	python3 -m venv .

3. pip install -r requirements.txt

4. flask run

5. go to localhost:5000

(*Note: Data is only for demo only, not accurate. Using sample for Release 2.13)
